databases:
  - name: maju_bersama_db
    databaseName: maju_bersama_production
    user: maju_bersama
    plan: free

services:
  - type: web
    name: maju_bersama_app
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "bundle exec rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "bundle exec rails server"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: maju_bersama_db
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
        value: d1372afa8cfafed81462985d6880104c
      - key: MAJU_BERSAMA_DATABASE_PASSWORD
        value: xPPWJuZ4EoAqfOzcqmVVeXgyfCP90XEi
      - key: WEB_CONCURRENCY
        value: 2
